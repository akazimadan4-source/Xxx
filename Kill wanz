--[[
    WANZZ STORE | SOUTH BRONX KILL AURA
    GUI Modern Glassmorphism
]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")

-- ===== SETTINGS =====
local Settings = {
    Enabled = false,
    Range = 150,
    Whitelist = {},
    AttackRate = 0.1
}

-- ===== ATTACK FUNCTION (South Bronx) =====
local function Attack(target)
    if not target or not target.Character then return end
    local remote = game:GetService("ReplicatedStorage"):FindFirstChild("AttackRemote")
        or game:GetService("ReplicatedStorage"):FindFirstChild("Damage")
        or game:GetService("ReplicatedStorage"):FindFirstChild("Hit")
        or game:GetService("ReplicatedStorage"):FindFirstChild("Combat")
        or game:GetService("ReplicatedStorage"):FindFirstChild("RemoteEvent")
    if remote then
        pcall(function()
            remote:FireServer(target.Character.HumanoidRootPart)
        end)
    else
        pcall(mouse1click)
    end
end

-- ===== GET CLOSEST TARGET =====
local function GetClosestTarget()
    local char = LocalPlayer.Character
    if not char then return nil end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    local pos = root.Position
    local closest, dist = nil, Settings.Range
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local targetRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if targetRoot then
                local d = (pos - targetRoot.Position).Magnitude
                if d <= Settings.Range then
                    local whitelisted = false
                    for _, name in ipairs(Settings.Whitelist) do
                        if name == player.Name then whitelisted = true break end
                    end
                    if not whitelisted and d < dist then
                        closest = player
                        dist = d
                    end
                end
            end
        end
    end
    return closest
end

-- ===== CREATE MODERN GUI =====
local function CreateGUI()
    local old = CoreGui:FindFirstChild("WanzzKillAura")
    if old then old:Destroy() end

    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "WanzzKillAura"
    screenGui.Parent = CoreGui

    -- Main Container (glass)
    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 320, 0, 240)
    main.Position = UDim2.new(0.5, -160, 0.5, -120)
    main.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
    main.BackgroundTransparency = 0.2
    main.BorderSizePixel = 0
    main.ClipsDescendants = true
    main.Parent = screenGui

    -- Corner radius
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 16)
    corner.Parent = main

    -- Glass overlay (transparan)
    local glass = Instance.new("Frame")
    glass.Size = UDim2.new(1, 0, 1, 0)
    glass.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    glass.BackgroundTransparency = 0.92
    glass.BorderSizePixel = 0
    glass.Parent = main

    -- Title
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -30, 0, 40)
    title.Position = UDim2.new(0, 15, 0, 12)
    title.BackgroundTransparency = 1
    title.Text = "✦ WANZZ STORE"
    title.TextColor3 = Color3.fromRGB(220, 220, 255)
    title.Font = Enum.Font.SourceSansBold
    title.TextSize = 18
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = main

    -- Subtitle
    local sub = Instance.new("TextLabel")
    sub.Size = UDim2.new(1, -30, 0, 18)
    sub.Position = UDim2.new(0, 15, 0, 48)
    sub.BackgroundTransparency = 1
    sub.Text = "South Bronx · Kill Aura"
    sub.TextColor3 = Color3.fromRGB(160, 160, 190)
    sub.Font = Enum.Font.SourceSans
    sub.TextSize = 12
    sub.TextXAlignment = Enum.TextXAlignment.Left
    sub.Parent = main

    -- Toggle (Slider) - Enabled
    local toggleBg = Instance.new("Frame")
    toggleBg.Size = UDim2.new(0, 50, 0, 28)
    toggleBg.Position = UDim2.new(1, -65, 0, 18)
    toggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    toggleBg.BorderSizePixel = 0
    toggleBg.Parent = main
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 14)
    toggleCorner.Parent = toggleBg

    local toggleCircle = Instance.new("Frame")
    toggleCircle.Size = UDim2.new(0, 22, 0, 22)
    toggleCircle.Position = UDim2.new(0, 3, 0, 3)
    toggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    toggleCircle.BorderSizePixel = 0
    toggleCircle.Parent = toggleBg
    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(0, 11)
    circleCorner.Parent = toggleCircle

    -- Fungsi toggle
    local function UpdateToggle()
        local targetPos = Settings.Enabled and 25 or 3
        local bgColor = Settings.Enabled and Color3.fromRGB(0, 180, 255) or Color3.fromRGB(60, 60, 80)
        TweenService:Create(toggleCircle, TweenInfo.new(0.2), {Position = UDim2.new(0, targetPos, 0, 3)}):Play()
        TweenService:Create(toggleBg, TweenInfo.new(0.2), {BackgroundColor3 = bgColor}):Play()
    end

    toggleBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            Settings.Enabled = not Settings.Enabled
            UpdateToggle()
        end
    end)
    UpdateToggle()

    -- Range Slider
    local rangeLabel = Instance.new("TextLabel")
    rangeLabel.Size = UDim2.new(0, 60, 0, 20)
    rangeLabel.Position = UDim2.new(0, 15, 0, 85)
    rangeLabel.BackgroundTransparency = 1
    rangeLabel.Text = "Range"
    rangeLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
    rangeLabel.Font = Enum.Font.SourceSans
    rangeLabel.TextSize = 14
    rangeLabel.TextXAlignment = Enum.TextXAlignment.Left
    rangeLabel.Parent = main

    local rangeValue = Instance.new("TextLabel")
    rangeValue.Size = UDim2.new(0, 40, 0, 20)
    rangeValue.Position = UDim2.new(1, -55, 0, 85)
    rangeValue.BackgroundTransparency = 1
    rangeValue.Text = tostring(Settings.Range)
    rangeValue.TextColor3 = Color3.fromRGB(255, 255, 255)
    rangeValue.Font = Enum.Font.SourceSansBold
    rangeValue.TextSize = 14
    rangeValue.TextXAlignment = Enum.TextXAlignment.Right
    rangeValue.Parent = main

    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(1, -100, 0, 4)
    sliderBg.Position = UDim2.new(0, 70, 0, 108)
    sliderBg.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = main
    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 2)
    sliderCorner.Parent = sliderBg

    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new((Settings.Range - 50) / 250, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBg
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(0, 2)
    fillCorner.Parent = sliderFill

    -- Slider drag (sederhana)
    local draggingRange = false
    local function UpdateRange(mouseX)
        local absPos = sliderBg.AbsolutePosition.X
        local size = sliderBg.AbsoluteSize.X
        local newX = math.clamp(mouseX - absPos, 0, size)
        local newVal = math.floor(50 + (newX / size) * 250)
        newVal = math.clamp(newVal, 50, 300)
        Settings.Range = newVal
        rangeValue.Text = tostring(newVal)
        sliderFill.Size = UDim2.new(newX / size, 0, 1, 0)
    end

    sliderBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingRange = true
            UpdateRange(input.Position.X)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingRange = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if draggingRange and input.UserInputType == Enum.UserInputType.MouseMovement then
            UpdateRange(input.Position.X)
        end
    end)

    -- Info label
    local info = Instance.new("TextLabel")
    info.Size = UDim2.new(1, -30, 0, 20)
    info.Position = UDim2.new(0, 15, 0, 130)
    info.BackgroundTransparency = 1
    info.Text = "Players: 0 · Whitelist: 0"
    info.TextColor3 = Color3.fromRGB(180, 180, 200)
    info.Font = Enum.Font.SourceSans
    info.TextSize = 13
    info.TextXAlignment = Enum.TextXAlignment.Left
    info.Parent = main

    -- Whitelist button
    local wlBtn = Instance.new("TextButton")
    wlBtn.Size = UDim2.new(1, -30, 0, 36)
    wlBtn.Position = UDim2.new(0, 15, 0, 165)
    wlBtn.BackgroundColor3 = Color3.fromRGB(40, 45, 60)
    wlBtn.BackgroundTransparency = 0.5
    wlBtn.Text = "✚  Whitelist Manager"
    wlBtn.TextColor3 = Color3.fromRGB(200, 200, 230)
    wlBtn.Font = Enum.Font.SourceSans
    wlBtn.TextSize = 14
    wlBtn.BorderSizePixel = 0
    wlBtn.Parent = main
    local wlCorner = Instance.new("UICorner")
    wlCorner.CornerRadius = UDim.new(0, 8)
    wlCorner.Parent = wlBtn

    -- ===== POPUP WHITELIST =====
    local popup = Instance.new("Frame")
    popup.Size = UDim2.new(0, 260, 0, 180)
    popup.Position = UDim2.new(0.5, -130, 0.5, -90)
    popup.BackgroundColor3 = Color3.fromRGB(18, 20, 26)
    popup.BackgroundTransparency = 0.15
    popup.BorderSizePixel = 0
    popup.Visible = false
    popup.Parent = screenGui
    local popCorner = Instance.new("UICorner")
    popCorner.CornerRadius = UDim.new(0, 16)
    popCorner.Parent = popup

    -- glass popup
    local popGlass = Instance.new("Frame")
    popGlass.Size = UDim2.new(1, 0, 1, 0)
    popGlass.BackgroundColor3 = Color3.fromRGB(255,255,255)
    popGlass.BackgroundTransparency = 0.93
    popGlass.BorderSizePixel = 0
    popGlass.Parent = popup

    local popTitle = Instance.new("TextLabel")
    popTitle.Size = UDim2.new(1, -20, 0, 35)
    popTitle.Position = UDim2.new(0, 10, 0, 8)
    popTitle.BackgroundTransparency = 1
    popTitle.Text = "Whitelist"
    popTitle.TextColor3 = Color3.fromRGB(220,220,255)
    popTitle.Font = Enum.Font.SourceSansBold
    popTitle.TextSize = 16
    popTitle.TextXAlignment = Enum.TextXAlignment.Left
    popTitle.Parent = popup

    local listFrame = Instance.new("ScrollingFrame")
    listFrame.Size = UDim2.new(1, -20, 0, 80)
    listFrame.Position = UDim2.new(0, 10, 0, 48)
    listFrame.BackgroundColor3 = Color3.fromRGB(30, 32, 40)
    listFrame.BackgroundTransparency = 0.4
    listFrame.BorderSizePixel = 0
    listFrame.Parent = popup
    local listCorner = Instance.new("UICorner")
    listCorner.CornerRadius = UDim.new(0, 8)
    listCorner.Parent = listFrame

    local listLayout = Instance.new("UIListLayout")
    listLayout.SortOrder = Enum.SortOrder.Name
    listLayout.Padding = UDim.new(0, 4)
    listLayout.Parent = listFrame

    local function RefreshWhitelist()
        for _, child in ipairs(listFrame:GetChildren()) do
            if child:IsA("TextButton") then child:Destroy() end
        end
        for _, name in ipairs(Settings.Whitelist) do
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -10, 0, 28)
            btn.BackgroundColor3 = Color3.fromRGB(45, 48, 60)
            btn.BackgroundTransparency = 0.4
            btn.Text = name .. "  ❌"
            btn.TextColor3 = Color3.fromRGB(220,220,240)
            btn.Font = Enum.Font.SourceSans
            btn.TextSize = 13
            btn.BorderSizePixel = 0
            btn.Parent = listFrame
            local btnCorner = Instance.new("UICorner")
            btnCorner.CornerRadius = UDim.new(0, 6)
            btnCorner.Parent = btn
            btn.MouseButton1Click:Connect(function()
                for i, v in ipairs(Settings.Whitelist) do
                    if v == name then table.remove(Settings.Whitelist, i) break end
                end
                RefreshWhitelist()
                UpdateInfo()
            end)
        end
    end

    local addBox = Instance.new("TextBox")
    addBox.Size = UDim2.new(0, 140, 0, 30)
    addBox.Position = UDim2.new(0, 10, 0, 138)
    addBox.PlaceholderText = "Nama player"
    addBox.BackgroundColor3 = Color3.fromRGB(40, 44, 56)
    addBox.BackgroundTransparency = 0.3
    addBox.TextColor3 = Color3.fromRGB(255,255,255)
    addBox.Font = Enum.Font.SourceSans
    addBox.TextSize = 13
    addBox.BorderSizePixel = 0
    addBox.Parent = popup
    local addCorner = Instance.new("UICorner")
    addCorner.CornerRadius = UDim.new(0, 8)
    addCorner.Parent = addBox

    local addBtn = Instance.new("TextButton")
    addBtn.Size = UDim2.new(0, 70, 0, 30)
    addBtn.Position = UDim2.new(1, -80, 0, 138)
    addBtn.Text = "Add"
    addBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
    addBtn.TextColor3 = Color3.fromRGB(255,255,255)
    addBtn.Font = Enum.Font.SourceSansBold
    addBtn.TextSize = 14
    addBtn.BorderSizePixel = 0
    addBtn.Parent = popup
    local addCorner2 = Instance.new("UICorner")
    addCorner2.CornerRadius = UDim.new(0, 8)
    addCorner2.Parent = addBtn
    addBtn.MouseButton1Click:Connect(function()
        local name = addBox.Text
        if name ~= "" then
            local dup = false
            for _, v in ipairs(Settings.Whitelist) do if v == name then dup = true break end end
            if not dup then
                table.insert(Settings.Whitelist, name)
                RefreshWhitelist()
                addBox.Text = ""
                UpdateInfo()
            end
        end
    end)

    -- Close popup ketika klik di luar
    popup.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            local pos = input.Position
            local absPos = popup.AbsolutePosition
            local size = popup.AbsoluteSize
            if not (pos.X >= absPos.X and pos.X <= absPos.X + size.X and pos.Y >= absPos.Y and pos.Y <= absPos.Y + size.Y) then
                popup.Visible = false
            end
        end
    end)

    wlBtn.MouseButton1Click:Connect(function()
        RefreshWhitelist()
        popup.Visible = true
    end)

    -- ===== UPDATE INFO =====
    local function UpdateInfo()
        local count = 0
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local pos = char.HumanoidRootPart.Position
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (pos - player.Character.HumanoidRootPart.Position).Magnitude
                    if dist <= Settings.Range then count = count + 1 end
                end
            end
        end
        info.Text = "Players: " .. count .. " · Whitelist: " .. #Settings.Whitelist
    end

    -- ===== HEARTBEAT =====
    local lastAttack = 0
    RunService.Heartbeat:Connect(function(dt)
        UpdateInfo()
        if not Settings.Enabled then return end
        lastAttack = lastAttack + dt
        if lastAttack < Settings.AttackRate then return end
        lastAttack = 0
        local target = GetClosestTarget()
        if target then Attack(target) end
    end)

    -- Drag main
    local drag, start, pos
    main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            drag = true
            start = input.Position
            pos = main.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then drag = false end
            end)
        end
    end)
    main.InputChanged:Connect(function(input)
        if drag and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - start
            main.Position = UDim2.new(pos.X.Scale, pos.X.Offset + delta.X, pos.Y.Scale, pos.Y.Offset + delta.Y)
        end
    end)
end

-- ===== START =====
pcall(CreateGUI)
print("[WANZZ] Modern GUI Kill Aura loaded.")
